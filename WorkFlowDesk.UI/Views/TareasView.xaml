<UserControl x:Class="WorkFlowDesk.UI.Views.TareasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:WorkFlowDesk.UI.Controls"
             xmlns:local="clr-namespace:WorkFlowDesk.UI.Behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Tareas" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center"
                       Margin="0,0,20,0"/>
            <Button Content="Nueva Tarea" 
                    Command="{Binding CrearTareaCommand}"
                    Visibility="{Binding CanManage, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Padding="15,8"
                    Margin="0,0,10,0"/>
            <Button Content="Exportar CSV"
                    Command="{Binding ExportarCommand}"
                    Visibility="{Binding CanExport, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Padding="15,8"
                    Margin="0,0,10,0"/>
            <TextBox local:TextBoxBindingHelper.BoundText="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}"
                     Width="180"
                     Margin="0,0,10,0"
                     Padding="5"
                     ToolTip="Buscar por título o descripción"/>
            <ComboBox ItemsSource="{Binding FiltrosEstado}"
                      SelectedItem="{Binding FiltroEstadoSeleccionado}"
                      DisplayMemberPath="Etiqueta"
                      Width="150"
                      Margin="10,0,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
        <DataGrid ItemsSource="{Binding Tareas}"
                  Visibility="{Binding HayElementos, Converter={StaticResource BooleanToVisibilityConverter}}"
                  SelectedItem="{Binding TareaSeleccionada}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Título" Binding="{Binding Titulo}" Width="*"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="2*"/>
                <DataGridTemplateColumn Header="Prioridad" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <Binding Path="Prioridad" Converter="{StaticResource EnumToStringConverter}"/>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Estado" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <Binding Path="Estado" Converter="{StaticResource EnumToStringConverter}"/>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Fecha Vencimiento" Binding="{Binding FechaVencimiento, StringFormat=dd/MM/yyyy}" Width="120"/>
                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Editar" 
                                        Command="{Binding DataContext.EditarTareaCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Visibility="{Binding DataContext.CanManage, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}"
                                        Margin="5,0"
                                        Padding="10,5"/>
                                <Button Content="Eliminar" 
                                        Command="{Binding DataContext.EliminarTareaCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Visibility="{Binding DataContext.CanManage, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}"
                                        Margin="5,0"
                                        Padding="10,5"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <controls:EmptyStateView Title="Sin tareas"
                                 Message="No hay tareas registradas o ninguna coincide con los filtros aplicados."
                                 Visibility="{Binding MostrarEstadoVacio, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <controls:LoadingIndicator Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock Text="{Binding InfoPaginacion}"
                       VerticalAlignment="Center"
                       Foreground="#7F8C8D"
                       Margin="0,0,15,0"/>
            <Button Content="Anterior"
                    Command="{Binding PaginaAnteriorCommand}"
                    Padding="12,6"
                    Margin="0,0,8,0"/>
            <Button Content="Siguiente"
                    Command="{Binding PaginaSiguienteCommand}"
                    Padding="12,6"/>
        </StackPanel>
    </Grid>
</UserControl>
