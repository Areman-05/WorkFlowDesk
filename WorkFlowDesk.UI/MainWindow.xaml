<Window x:Class="WorkFlowDesk.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkFlowDesk.UI"
        mc:Ignorable="d"
        Title="WorkFlowDesk" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Window.InputBindings>
        <KeyBinding Key="D1" Modifiers="Control" Command="{Binding NavigateToDashboardCommand}"/>
        <KeyBinding Key="D2" Modifiers="Control" Command="{Binding NavigateToEmpleadosCommand}"/>
        <KeyBinding Key="D3" Modifiers="Control" Command="{Binding NavigateToProyectosCommand}"/>
        <KeyBinding Key="D4" Modifiers="Control" Command="{Binding NavigateToTareasCommand}"/>
        <KeyBinding Key="D5" Modifiers="Control" Command="{Binding NavigateToClientesCommand}"/>
        <KeyBinding Key="D6" Modifiers="Control" Command="{Binding NavigateToReportesCommand}"/>
        <KeyBinding Key="D7" Modifiers="Control" Command="{Binding NavigateToConfiguracionCommand}"/>
        <KeyBinding Key="L" Modifiers="Control" Command="{Binding LogoutCommand}"/>
    </Window.InputBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#2C3E50">
            <DockPanel LastChildFill="True">
                <Border DockPanel.Dock="Bottom"
                        Background="#243342"
                        Padding="12"
                        Margin="8,0,8,8">
                    <StackPanel>
                        <TextBlock Text="{Binding UserName}"
                                   Foreground="White"
                                   FontWeight="SemiBold"
                                   FontSize="13"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding UserRole}"
                                   Foreground="#BDC3C7"
                                   FontSize="11"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <StackPanel Margin="0,0,0,0">
                <TextBlock Text="WorkFlowDesk" 
                           Foreground="White" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Margin="15,20,15,30"
                           HorizontalAlignment="Center"/>
                
                <Button Content="Dashboard" 
                        Style="{StaticResource SidebarButtonStyle}"
                        Command="{Binding NavigateToDashboardCommand}"
                        ToolTip="Panel principal (Ctrl+1)"
                        Margin="10,5"/>
                <Button Content="Empleados" 
                        Style="{StaticResource SidebarButtonStyle}"
                        Command="{Binding NavigateToEmpleadosCommand}"
                        Visibility="{Binding ShowEmpleados, Converter={StaticResource BooleanToVisibilityConverter}}"
                        ToolTip="Gestión de empleados (Ctrl+2)"
                        Margin="10,5"/>
                <Button Content="Proyectos" 
                        Style="{StaticResource SidebarButtonStyle}"
                        Command="{Binding NavigateToProyectosCommand}"
                        Visibility="{Binding ShowProyectos, Converter={StaticResource BooleanToVisibilityConverter}}"
                        ToolTip="Gestión de proyectos (Ctrl+3)"
                        Margin="10,5"/>
                <Button Content="Tareas" 
                        Style="{StaticResource SidebarButtonStyle}"
                        Command="{Binding NavigateToTareasCommand}"
                        ToolTip="Gestión de tareas (Ctrl+4)"
                        Margin="10,5"/>
                <Button Content="Clientes" 
                        Style="{StaticResource SidebarButtonStyle}"
                        Command="{Binding NavigateToClientesCommand}"
                        Visibility="{Binding ShowClientes, Converter={StaticResource BooleanToVisibilityConverter}}"
                        ToolTip="Gestión de clientes (Ctrl+5)"
                        Margin="10,5"/>
                <Button Content="Reportes" 
                        Style="{StaticResource SidebarButtonStyle}"
                        Command="{Binding NavigateToReportesCommand}"
                        Visibility="{Binding ShowReportes, Converter={StaticResource BooleanToVisibilityConverter}}"
                        ToolTip="Estadísticas y exportación (Ctrl+6)"
                        Margin="10,5"/>
                <Button Content="Configuración" 
                        Style="{StaticResource SidebarButtonStyle}"
                        Command="{Binding NavigateToConfiguracionCommand}"
                        Visibility="{Binding ShowConfiguracion, Converter={StaticResource BooleanToVisibilityConverter}}"
                        ToolTip="Backup, BD y preferencias (Ctrl+7)"
                        Margin="10,5"/>

                <Button Content="Cerrar sesión"
                        Style="{StaticResource SidebarButtonStyle}"
                        Command="{Binding LogoutCommand}"
                        ToolTip="Cerrar sesión actual (Ctrl+L)"
                        Margin="10,20,10,10"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1">
            <ContentControl x:Name="ContentArea"/>
        </Grid>
    </Grid>
</Window>
