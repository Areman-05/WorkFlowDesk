<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Paleta Stitch / Material -->
    <Color x:Key="StitchPrimaryColor">#9E00B5</Color>
    <Color x:Key="StitchPrimaryContainerColor">#C028D7</Color>
    <Color x:Key="StitchSecondaryColor">#944A00</Color>
    <Color x:Key="StitchSecondaryContainerColor">#FD933D</Color>
    <Color x:Key="StitchBackgroundColor">#FCF8FF</Color>
    <Color x:Key="StitchSurfaceColor">#FCF8FF</Color>
    <Color x:Key="StitchSurfaceContainerLowColor">#F6F2FF</Color>
    <Color x:Key="StitchSurfaceContainerColor">#EFEBFF</Color>
    <Color x:Key="StitchOnSurfaceColor">#181445</Color>
    <Color x:Key="StitchOnSurfaceVariantColor">#524251</Color>
    <Color x:Key="StitchSidebarColor">#36003E</Color>
    <Color x:Key="StitchOutlineVariantColor">#D6C0D3</Color>
    <Color x:Key="StitchErrorColor">#BA1A1A</Color>
    <Color x:Key="StitchSuccessColor">#22C55E</Color>
    <Color x:Key="StitchTertiaryColor">#87466D</Color>
    <Color x:Key="StitchTertiaryContainerColor">#A45E86</Color>
    <Color x:Key="StitchOnTertiaryContainerColor">#FFFBFF</Color>
    <Color x:Key="StitchOnPrimaryContainerColor">#FFFBFF</Color>
    <Color x:Key="StitchOnSecondaryContainerColor">#693300</Color>

    <SolidColorBrush x:Key="StitchPrimaryBrush" Color="{StaticResource StitchPrimaryColor}"/>
    <SolidColorBrush x:Key="StitchPrimaryContainerBrush" Color="{StaticResource StitchPrimaryContainerColor}"/>
    <SolidColorBrush x:Key="StitchSecondaryBrush" Color="{StaticResource StitchSecondaryColor}"/>
    <SolidColorBrush x:Key="StitchSecondaryContainerBrush" Color="{StaticResource StitchSecondaryContainerColor}"/>
    <SolidColorBrush x:Key="StitchBackgroundBrush" Color="{StaticResource StitchBackgroundColor}"/>
    <SolidColorBrush x:Key="StitchSurfaceBrush" Color="{StaticResource StitchSurfaceColor}"/>
    <SolidColorBrush x:Key="StitchSurfaceContainerBrush" Color="{StaticResource StitchSurfaceContainerColor}"/>
    <SolidColorBrush x:Key="StitchSurfaceContainerLowBrush" Color="{StaticResource StitchSurfaceContainerLowColor}"/>
    <SolidColorBrush x:Key="StitchOnSurfaceBrush" Color="{StaticResource StitchOnSurfaceColor}"/>
    <SolidColorBrush x:Key="StitchOnSurfaceVariantBrush" Color="{StaticResource StitchOnSurfaceVariantColor}"/>
    <SolidColorBrush x:Key="StitchSidebarBrush" Color="{StaticResource StitchSidebarColor}"/>
    <SolidColorBrush x:Key="StitchOutlineVariantBrush" Color="{StaticResource StitchOutlineVariantColor}"/>
    <SolidColorBrush x:Key="StitchErrorBrush" Color="{StaticResource StitchErrorColor}"/>
    <SolidColorBrush x:Key="StitchSuccessBrush" Color="{StaticResource StitchSuccessColor}"/>
    <SolidColorBrush x:Key="StitchTertiaryBrush" Color="{StaticResource StitchTertiaryColor}"/>
    <SolidColorBrush x:Key="StitchTertiaryContainerBrush" Color="{StaticResource StitchTertiaryContainerColor}"/>
    <SolidColorBrush x:Key="StitchOnTertiaryContainerBrush" Color="{StaticResource StitchOnTertiaryContainerColor}"/>
    <SolidColorBrush x:Key="StitchOnPrimaryContainerBrush" Color="{StaticResource StitchOnPrimaryContainerColor}"/>
    <SolidColorBrush x:Key="StitchOnSecondaryContainerBrush" Color="{StaticResource StitchOnSecondaryContainerColor}"/>
    <SolidColorBrush x:Key="StitchWhiteBrush" Color="White"/>

    <!-- Degradado fondo principal -->
    <LinearGradientBrush x:Key="StitchBackgroundGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FDF4FF" Offset="0"/>
        <GradientStop Color="#EDE9FE" Offset="0.5"/>
        <GradientStop Color="#FCF8FF" Offset="1"/>
    </LinearGradientBrush>

    <RadialGradientBrush x:Key="StitchFogBackgroundBrush" GradientOrigin="0.5,0.5" Center="0.5,0.5" RadiusX="0.85" RadiusY="0.85">
        <GradientStop Color="#FCF8FF" Offset="0"/>
        <GradientStop Color="#E9E5FF" Offset="1"/>
    </RadialGradientBrush>

    <!-- Tarjeta cristal -->
    <Style x:Key="GlassCardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="#D9FFFFFF"/>
        <Setter Property="BorderBrush" Value="#66FFFFFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="32" ShadowDepth="0" Opacity="0.08" Color="#2E1065"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modal Stitch (Nuevo Empleado, etc.) — panel opaco, sin glass -->
    <Style x:Key="StitchModalGlassPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource StitchSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource StitchOutlineVariantBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="32" ShadowDepth="4" Opacity="0.12" Color="#2E1065" Direction="270"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StitchModalFieldBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource StitchSurfaceContainerLowBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource StitchOutlineVariantBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
    </Style>

    <Style x:Key="StitchModalCancelButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="Background" Value="{StaticResource StitchSurfaceContainerLowBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource StitchOutlineVariantBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="24,10"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource StitchSurfaceContainerBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StitchAuthGlassCardStyle" TargetType="Border" BasedOn="{StaticResource GlassCardBorderStyle}">
        <Setter Property="Background" Value="#D9FFFFFF"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="32"/>
    </Style>

    <Style x:Key="StitchAuthRegisterGlassCardStyle" TargetType="Border" BasedOn="{StaticResource StitchAuthGlassCardStyle}">
        <Setter Property="CornerRadius" Value="24"/>
        <Setter Property="BorderBrush" Value="#66FFFFFF"/>
    </Style>

    <Style x:Key="StitchAuthGlassSubtleStyle" TargetType="Border">
        <Setter Property="Background" Value="#33FFFFFF"/>
        <Setter Property="BorderBrush" Value="#66FFFFFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="16"/>
    </Style>

    <Style x:Key="StitchAuthFieldBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="#80FCF8FF"/>
        <Setter Property="BorderBrush" Value="#4DD6C0D3"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
    </Style>

    <Style x:Key="StitchAuthInnerTextBoxStyle" TargetType="TextBox">
        <Setter Property="FlowDirection" Value="LeftToRight"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="48,14,16,14"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="MinHeight" Value="48"/>
    </Style>

    <Style x:Key="StitchModalInnerTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource StitchAuthInnerTextBoxStyle}">
        <Setter Property="Padding" Value="40,10,16,10"/>
        <Setter Property="MinHeight" Value="40"/>
    </Style>

    <Style x:Key="StitchAuthInnerPasswordBoxStyle" TargetType="PasswordBox">
        <Setter Property="FlowDirection" Value="LeftToRight"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="48,14,16,14"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="MinHeight" Value="48"/>
    </Style>

    <Style x:Key="StitchAuthComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="48,14,40,14"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="MinHeight" Value="48"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid SnapsToDevicePixels="True">
                        <ToggleButton x:Name="ToggleButton"
                                      Focusable="False"
                                      ClickMode="Press"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="Transparent"/>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="ContentSite"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          IsHitTestVisible="False"/>
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="{StaticResource StitchSurfaceBrush}"
                                        BorderBrush="{StaticResource StitchOutlineVariantBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                    <ScrollViewer Margin="4,6">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StitchModalComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource StitchAuthComboBoxStyle}">
        <Setter Property="Padding" Value="40,10,36,10"/>
        <Setter Property="MinHeight" Value="40"/>
    </Style>

    <!-- Botón primario Stitch (degradado) -->
    <Style x:Key="StitchPrimaryButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#9E00B5" Offset="0"/>
                                <GradientStop Color="#C028D7" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.92"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StitchAuthPrimaryButtonLargeStyle" TargetType="Button" BasedOn="{StaticResource StitchPrimaryButtonStyle}">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="16,16"/>
    </Style>

    <Style x:Key="StitchAuthSecondaryButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Padding" Value="16,16"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="#F6F2FF"
                            BorderBrush="#33D6C0D3"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#EFEBFF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StitchAuthCancelButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="16,16"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="#33FFFFFF"
                            BorderBrush="#33D6C0D3"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#66FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StitchPrimaryButtonXlStyle" TargetType="Button" BasedOn="{StaticResource StitchPrimaryButtonStyle}">
        <Setter Property="Padding" Value="32,12"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" CornerRadius="16" Padding="{TemplateBinding Padding}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#9E00B5" Offset="0"/>
                                <GradientStop Color="#C028D7" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.92"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StitchSecondaryGlassButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="24,12"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="#33FFFFFF"
                            BorderBrush="#33847183"
                            BorderThickness="1"
                            CornerRadius="16"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#66FFFFFF"/>
                            <Setter TargetName="Bd" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.12" Color="#2E0A65"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StitchExportPillButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="24,14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="White"
                            BorderBrush="#E2E8F0"
                            BorderThickness="1"
                            CornerRadius="20"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#FAFAFA"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#CBD5E1"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StitchAuthOutlineButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="#33FFFFFF"
                            BorderBrush="#33D6C0D3"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#66FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Sidebar nav item -->
    <Style x:Key="StitchSidebarNavButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#CCF3EEFF"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#1A9E00B5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Sidebar nav activo -->
    <Style x:Key="StitchSidebarNavActiveButtonStyle" TargetType="Button" BasedOn="{StaticResource StitchSidebarNavButtonStyle}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border Width="4" HorizontalAlignment="Left" Background="{StaticResource StitchPrimaryBrush}"/>
                        <Border Background="#1A9E00B5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Ghost / link button -->
    <Style x:Key="StitchGhostButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource StitchPrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Tipografía -->
    <Style x:Key="StitchH1TextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
    </Style>
    <Style x:Key="StitchH2TextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
    </Style>
    <Style x:Key="StitchH3TextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
    </Style>
    <Style x:Key="StitchBodyTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceVariantBrush}"/>
    </Style>
    <Style x:Key="StitchCaptionTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceVariantBrush}"/>
    </Style>
    <Style x:Key="StitchLabelTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceVariantBrush}"/>
    </Style>

    <!-- Stat card number -->
    <Style x:Key="StitchStatNumberStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
    </Style>

    <!-- Calendario modal (DatePicker) -->
    <Style x:Key="StitchCalendarButtonStyle" TargetType="CalendarButton">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="MinWidth" Value="32"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CalendarButton">
                    <Border x:Name="Bg" Background="Transparent" CornerRadius="8" Padding="6,4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#1A9E00B5"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#339E00B5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StitchCalendarDayButtonStyle" TargetType="CalendarDayButton">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="MinWidth" Value="34"/>
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="Margin" Value="1"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CalendarDayButton">
                    <Grid>
                        <Border x:Name="Bg" CornerRadius="17" Background="Transparent" BorderThickness="0"/>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource StitchPrimaryBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsToday" Value="True"/>
                                <Condition Property="IsSelected" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Bg" Property="BorderBrush" Value="{StaticResource StitchPrimaryBrush}"/>
                            <Setter TargetName="Bg" Property="BorderThickness" Value="1.5"/>
                        </MultiTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#1A9E00B5"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bg" Property="BorderBrush" Value="{StaticResource StitchPrimaryBrush}"/>
                            <Setter TargetName="Bg" Property="BorderThickness" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#99524251"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StitchCalendarStyle" TargetType="Calendar">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="{StaticResource StitchSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource StitchOutlineVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource StitchOnSurfaceBrush}"/>
        <Setter Property="CalendarDayButtonStyle" Value="{StaticResource StitchCalendarDayButtonStyle}"/>
        <Setter Property="CalendarButtonStyle" Value="{StaticResource StitchCalendarButtonStyle}"/>
    </Style>

</ResourceDictionary>
